# This used to include several generic targets to work with GPG and SSH, but was split up to reorganize things. Now, there are:
# - `91-ansible-vault.mk` that manipulates Ansible Vault
# - `92-ssh.mk` that manipulates SSH procedures
# - `93-gpg.mk` that manipulates GnuPG
